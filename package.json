{
  "name": "@becode/js-intro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx seve -e '*.json,*.md,node_modules,shared' -p 8000 -i .",
    "api": "npx json-server ./shared/api.json"
  },
  "dependencies": {
    "json-server": "^0.16.1"
  },
  "devDependencies": {
    "seve": "^0.10.0"
  }
}
